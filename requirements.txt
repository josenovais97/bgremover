Django>=5.0,<5.2
django-environ==0.11.2
# Still used at BUILD time: `collectstatic` runs through
# config.storage.LenientManifestStaticFilesStorage, which is a WhiteNoise class.
# Nothing serves requests through WhiteNoise any more — the prerendered output is
# served by Cloudflare — so this is a build dependency, not a runtime one.
whitenoise==6.6.0
# Only for the Dockerfile's CMD, which still runs the Django app as a WSGI
# service. The deployed site does not use it: `manage.py prerender` turns the app
# into static files and no WSGI process runs in production.
gunicorn==21.2.0

# Optional: only needed if you enable the Django admin with PostgreSQL.
# psycopg2-binary==2.9.9
